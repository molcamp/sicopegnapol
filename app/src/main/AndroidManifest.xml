<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".tabs"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".login"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScanConductor" />
        <activity android:name=".ScanAcompañante" />
        <activity android:name=".login2" />
        <activity android:name=".registro2" />
        <activity android:name=".consulta" />
        <activity android:name=".persona" />
        <activity android:name=".vehiculo" />
        <activity android:name=".conductor" />
        <activity android:name=".acompanante" />

    </application>

</manifest>